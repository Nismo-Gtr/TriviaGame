import React from "react";
import "./jumbotron.css";

const Jumbotron = (props) => (
     <div className="size">
         <p></p>
            <h1 className="header">Trivia Time!</h1>
                {/* <button style={button} onClick={console.log(props)}>Test Click!</button> */}
     </div>
);

export default Jumbotron;